import * as merlin from "./merlin";
import * as remote from "./remote";
import * as types from "./types";
export interface ISettings {
    imandra: {
        codelens: {
            enabled: boolean;
            unicode: boolean;
        };
        debounce: {
            linter: number;
        };
        diagnostics: {
            merlinPerfLogging: boolean;
            tools: Array<"merlin" | "bsb" | "esy">;
        };
        path: {
            bsb: string;
            env: string;
            esy: string;
            ocamlfind: string;
            imandra_merlin: string;
            ocpindent: string;
            opam: string;
            rebuild: string;
            refmt: string;
            refmterr: string;
            rtop: string;
            switch: string;
        };
        format: {
            width: number | null;
        };
        server: {
            languages: Array<"imandra" | "imandra-reason">;
        };
    };
}
export declare namespace ISettings {
    const defaults: ISettings;
    function withDefaults(overrides: typeof defaults.imandra | undefined | null): typeof defaults.imandra;
}
export { merlin, remote, types };
